import DataTypes

class a:
    var1 = DataTypes.String(default='default', size=10)
    # var2 = DataTypes.Integer(auto_increment=True, primary_key=True) 

class abob:
    name = DataTypes.String(primary_key=True)
